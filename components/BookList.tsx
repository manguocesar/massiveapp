import React from 'react'

const BookList = (title) => {
    return (
        <section>
            <h2 className='font-bebas-neue text-4xl text-light-100'>{title.title}</h2>
        </section>
    )
}

export default BookList